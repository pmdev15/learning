#include<stdio.h>

int main(){
    int *ptr = NULL;
    return 0;
}

//      NULL POINTER
// A NULL pointer is a pointer that doesnot point to a memory location. It represents an invalid memory location.
// When a NULL value is assigned to a pointer , then the pointer is considered a null pointer.
//
//      USE OF NULL POINTER
// 1. it is used to initialize a pointer when that pointer isn't assigned any valid memory address yet.
// 2. usefull for handling errors while using malloc func
//
//      FACTS ABOUT NULL POINTER
// 1. NULL = 0 , we can use either one
// 2. Size of NULL pointer depends on the platform
